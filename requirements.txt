# Minimal, well-known dependencies only.
# Install with:  pip install -r requirements.txt

requests        # HTTP calls to OpenSanctions / EU list
python-dotenv   # loads API keys from the .env file
PyYAML          # reads rules.yaml and config files
websockets      # live AIS websocket stream (aisstream.io)
